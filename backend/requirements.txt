# Django framework
Django==4.2.4

# Django REST framework for building APIs
djangorestframework==3.14.0

# JWT Authentication for Django REST Framework
djangorestframework-simplejwt==5.2.2

# PostgreSQL Database adapter for Django
psycopg2-binary==2.9.6

# For handling environment variables securely
python-decouple==3.8

# Validation and form handling
django-cors-headers==3.14.0
django-crispy-forms==1.14.0

# Form validation with formik
django-rest-knox==4.2.0

# Pillow for image field handling (if needed)
Pillow==10.0.0

# Whitenoise for serving static files (in production)
whitenoise==6.4.0

# Gunicorn - production WSGI server (for deployment to services like Heroku or Azure)
gunicorn==20.1.0

# Other optional packages you may need
django-environ==0.10.0

# Security best practices and securing headers
django-secure==1.0.1
django-sslserver==0.24
